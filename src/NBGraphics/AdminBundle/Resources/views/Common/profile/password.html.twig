{% extends "NBGraphicsFrontSiteBundle::frontBaseElements.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}

    {% include 'NBGraphicsAdminBundle:Common:menu.html.twig' %}

    <div class="col-sm-12 col-md-9">
    <div class="box box-default">
        <div class="box-header with-border"> <h3 class="box-title">Edition profil</h3> </div>
        <div class="box-body box-edit">
        <div class="well">
        {{ form_start(form, { 'action': path('admin_user_password',{ 'id': entity.id }), 'attr': { 'class': 'fos_user_profile_password' } }) }}
        <div class="box-body">
            {{ form_widget(form) }}
        </div>
        {{ form_end(form) }}
        </div>
    </div>

    <div class="actions">
        <a href="{{ path('admin_profil') }}" class="btn btn-primary pull-left">
            <i class="fa fa-chevron-left" aria-hidden="true"></i> &nbsp; Retour à mon profil
        </a>
    </div>
    </div>
    </div>

{% endblock %}