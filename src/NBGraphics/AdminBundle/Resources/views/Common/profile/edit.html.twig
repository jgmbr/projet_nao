{% extends "NBGraphicsFrontSiteBundle::frontBaseElements.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}

    <div id="admin-wrapper">

        {% include 'NBGraphicsAdminBundle:Common:menu.html.twig' %}

        <div class="container edit-profil">

            <h2>Edition profil</h2>

            <div class="edit-form">
                {{ form_start(form, { 'action': path('admin_profil_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
                {{ form_widget(form) }}
                <input type="submit" value="{{ 'profile.edit.submit'|trans }}" class="btn btn-warning" />
                {{ form_end(form) }}
            </div>


            <div class="actions">
                <a href="{{ path('admin_profil') }}" class="btn btn-primary pull-left">
                    <i class="fa fa-chevron-left" aria-hidden="true"></i> &nbsp; Retour à mon profil
                </a>
            </div>

        </div>

    </div>

{% endblock %}