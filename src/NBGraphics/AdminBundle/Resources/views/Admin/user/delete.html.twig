{% extends "NBGraphicsFrontSiteBundle::frontBaseElements.html.twig" %}

{% block body %}

    {% include 'NBGraphicsAdminBundle:Common:menu.html.twig' %}

    <div class="container delete-user-admin">

        <h3>Suppression utilisateur</h3>
        <p> Etes-vous certain de vouloir supprimer l'utilisateur "{{ user.email }}" ? </p>

        {{ form_start(delete_form) }}
        <input type="submit" value="Oui, je confirme la suppression" class="btn btn-danger" />
        {{ form_rest(delete_form) }}
        {{ form_end(delete_form) }}


        <div class="actions">
            <a href="{{ path('user_index') }}" class="btn btn-primary pull-left">
                <i class="fa fa-chevron-left" aria-hidden="true"></i> &nbsp;
                Retour à la liste des membres
            </a>
        </div>

    </div>

{% endblock %}
