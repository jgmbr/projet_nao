{% extends "NBGraphicsFrontSiteBundle::frontBaseElements.html.twig" %}

{% block body %}

    <div id="admin-wrapper">

        {% include 'NBGraphicsAdminBundle:Common:menu.html.twig' %}

        <div class="container delete-observation">

            <h2>Supprimer une observation.</h2>

            <p><strong>Etes-vous certain de vouloir supprimer l'observation {{ observation.id }} ?</strong></p>



                {{ form_start(delete_form) }}
                    <input type="submit" value="Oui, je confirme la suppression" class="btn btn-danger" />
                {{ form_rest(delete_form) }}
                {{ form_end(delete_form) }}

                <div class="actions">
                    <a href="{{ path('admin_my_observation_index') }}" class="btn btn-primary pull-left">
                        <i class="fa fa-chevron-left" aria-hidden="true"></i> &nbsp; Retour à la liste des observations
                    </a>
                </div>

        </div>

    </div>

{% endblock %}
